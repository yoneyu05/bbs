<p id="notice"><%= notice %></p>

<h1>Delrequests</h1>

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Del</th>
      <th>Reason</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

      <tr>





      </tr>
    <% end %>
  </tbody>
</table>






<div class="row">
<div class="col-xs-5 col-sm-5 col-md-5 col-lg-8">
  <% @delrequests.each do |delrequest| %>
<table class="table table-condensed table-hover">
  <tbody>
        <tr class="topicshow">
      	<td   colspan="2">
          <b><%= delrequest.address %></b>
          <%= delrequest.del_id %>
          <%= topic.created_at %>
        </td>
        </tr>
        <tr class="topicshow">
          <td><%= delrequest.reason %></td>
        </tr>

        <% if user_signed_in? %>
        <tr>
        <td><%= link_to 'Edit', edit_delrequest_path(delrequest) %></td>
        <td><%= link_to 'Destroy', delrequest, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
  </tbody>
      <% end %>
</table>
    <% end %>
</div>






<br>
<%= render 'form', delrequest: @delrequest %>

<%= link_to 'New Delrequest', new_delrequest_path %>
