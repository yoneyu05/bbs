<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
<%= form_with(model: @newtopic, local: true) do |form| %>
  <% if @newtopic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newtopic.errors.count, "error") %> prohibited this topic from being saved:</h2>
      <ul>
      <% @newtopic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :タイトル %>
    <%= form.text_field :title, id: :topic_title, class: :"form-control" %>
  </div>

    <div class="field">
    <%= form.label :名前 %>
    <%= form.text_field :name, id: :topic_name, class: :"form-control" %>
  </div>

  <div class="field">
    <%= form.label :内容 %>
    <%= form.text_area :content, id: :topic_content, class: :"form-control" %>
  </div>

  <%= form.label :画像 %>
<%= form.file_field :image %>

<br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>
